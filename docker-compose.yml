version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - '8000:8000'
    volumes:
      - ./backend/people_pluse.db:/app/people_pluse.db
    environment:
      DATABASE_URL: sqlite:///./people_pluse.db
